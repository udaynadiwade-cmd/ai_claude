# Copy to linkedin/.env  (git-ignored). One line, nothing else needed.
# Paste the URL from the old 30-config.md — the one that took three attempts.
WEBHOOK_URL=

# Optional. Name shown in the post log / payload.
AUTHOR=Uday Nadiwade

# Catalog access — set ONE. See ACCESS.md. befach.com challenges scripts with
# Cloudflare Turnstile, so the writer cannot read product pages directly.
# CATALOG_EXPORT=/path/to/catalog.json
# CATALOG_API=https://befach.com/api/internal/products
# CATALOG_API_KEY=
# CATALOG_URL=https://befach.com
# CATALOG_BYPASS_TOKEN=
